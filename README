# MyShell

Simple shell written for an OS class assignment.

Building:
    To build, simply run `make` or `make -f Makefile`.

    Alternatively, CMake may be used with the provided,
    CMakeLists.txt file.
